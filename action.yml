name: Git URL Auth
description: Configure authenticated GitHub access through a Git URL rewrite

inputs:
  token:
    description: GitHub access token
    required: true
  repository:
    description: GitHub repository to configure
    required: true

runs:
  using: node24
  main: dist/main.cjs
  post: dist/post.cjs